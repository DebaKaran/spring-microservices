server:
  port: 8090
spring:
  application:
    name: "loans"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never
management:
  endpoints:
    web:
      exposure:
        include: busrefresh, info
  info:
    env:
      enabled: true