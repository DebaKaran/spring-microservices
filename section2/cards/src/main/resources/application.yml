server:
  port: 9000
spring:
  application:
    name: "cards"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never
  # config:
  #   import: "optional:configserver:"
management:
  endpoints:
    web:
      exposure:
        include: busrefresh, info
  info:
    env:
      enabled: true