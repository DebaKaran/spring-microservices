server:
  port: 8080
spring:
  application:
    name: accounts
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: never

# Learning-only: actuator shutdown endpoint
# Do NOT enable in production
management:
  endpoints:
    web:
      exposure:
        include: busrefresh, info, shutdown
  info:
    env:
      enabled: true
  endpoint:
    shutdown:
      enabled: true